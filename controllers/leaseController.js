const db = require("../models");
const express = require("express");
const app = express();

// Defining methods for the leaseController
module.exports = {
  // add lease
  create: function(req, res) {
    
  }

};